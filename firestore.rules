rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /registrations/{regId} {
      // Public read, create, and certificate field updates
      allow read:   if true;
      allow create: if true;
      allow update: if true;  // Certificate URL written back after generation
      allow delete: if false;
    }
  }
}
